apiVersion: v1
kind: Secret
metadata:
  name: dataguard-api-secrets
  namespace: dataguard
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_WITH_SECRET_MANAGER_REFERENCE"
  REDIS_URL: "REPLACE_WITH_SECRET_MANAGER_REFERENCE"
  SECRET_KEY: "REPLACE_WITH_RANDOM_SECRET"
  DATAGUARD_JWT_SECRET: "REPLACE_WITH_RANDOM_SECRET"
# This file is a contract/example only. Do not apply it with placeholder values
# and do not commit real credentials. Prefer External Secrets, Vault, or the
# cloud provider's managed secret integration in production.
